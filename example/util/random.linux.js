#!/usr/bin/env node.js



/*

var random = include('util/random');

var randomBINARY = random.binary();
var randomHEX = random.hex();
var randomBASE64 = random.base64();

console.debug(0, '[BINARY] (%d)\n%s\n\n[HEX] (%d)\n%s\n\n[BASE64] (%d)\n%s\n\n',
	randomBINARY.length, randomBINARY, randomHEX.length, randomHEX, randomBASE64.length, randomBASE64);

*/

/*^
o	zusammn mnit rechtern vim-fenster

	möchte ich noch random digits im decimalen und binären ausgehen.

	das dient dann Math.random():





		dachte mir .. ich erzeuge
		
			.. while(! length) { str += (bit||decimal);

				=> wenn 'decimal', dann langem dec-string (check max size=

					=> überelege: is bereits oder sollte evlt. auch random für große zahlen!?

						DANN WÄRE BESSER: 
						
						
			NE:
			
	==> =>	ich erzeuge einfach einen BIT-stream .. erstes ergebnis damit möglich;

	dann konvertiere ich mehrere bits zu verschiedenen .. zahlen?
	bzw. 

	evtl. große zahlen .. dann toString(_base/_radix)
	HÄ?





	(a) ich will (0/1) random bits ...
	(b) ich will (0/1/2) random numbers
	(c) (0/1/2/3) randoms ..
	etc.?!?


=>	idee war, dass ich efinach normale, decimale zahlen zufällig erzeuge
	und dann als string in "voller" länge umwandeln zu ergebnis-charset?


		//TODO/ *too stoned now*.


*/
